# Optional TfL Unified API application key.
# The app works WITHOUT a key at low request volumes. Register a free key at
# https://api-portal.tfl.gov.uk/ for higher rate limits, then copy this file to
# `.env` and fill it in. It is read at build time via Expo's EXPO_PUBLIC_ prefix.
EXPO_PUBLIC_TFL_APP_KEY=
